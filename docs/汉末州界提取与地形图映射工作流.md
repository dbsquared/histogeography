# 汉末十三州 · 州界提取与地形图映射工作流

> 以「幽州」为样板总结的可复用流程。下一个州（如冀州、并州、青州……）按本文替换输入文件和少量参数即可复现。
> 数据来源标注：地形底图源自地理空间数据云 (www.gscloud.cn) 的 SRTM DEM。

---

## 0. 总览

整条管线分三大步：

| 步骤 | 脚本 | 输入 | 输出 | 目的 |
|---|---|---|---|---|
| **Step 1 提取** | `step1_erase_text_youzhou.py` | `汉末十三州地图范例/<州>.png` | `<州>_step1/`（掩膜、去文字图、边界点 JSON） | 从彩色范例图里抠出该州色块，擦掉压在上面的文字/白线，得到干净的州界多边形 |
| **Step 2 配准** | 锚点表 `*_anchor_table.json` | 人工标注的同名地物点 | `(px,py)->(lon,lat)` 多项式 | 建立范例图像素坐标 ↔ 真实经纬度的映射 |
| **Step 3 叠加** | `overlay_youzhou_step1.py` + `zoom_overlay.py` | 边界点 JSON + 锚点表 + `china_full_v3.png` | `<州>_overlay_*/`（半透明叠加图） | 把州界映射到经纬度，再画到大地形图上核对形状 |

核心产物是 **边界点 JSON**（像素坐标）和 **锚点表**（像素↔经纬度）。有了这两样，任何后续渲染都能复算。

---

## 1. Step 1：色块提取 + 文字擦除 + 边界修补

对应脚本 `step1_erase_text_youzhou.py`，逐段说明。

### 1.1 严格颜色阈值取该州本体
- 幽州是**深绿**，用 RGB + 相对关系双重约束，避免把邻州浅绿/黄绿吃进来：
  ```python
  green_mask = (g > r+40) & (g > b+30) & (120<=g<=205) & (65<=r<=155) & (70<=b<=175)
  ```
- **换州第一件事**：先用取色脚本采样目标州的主色，改这里的阈值。每个州颜色不同（冀州偏黄、青州偏蓝紫等），这是唯一必须重标的部分。

### 1.2 缺陷检测（要擦掉的东西）
识别三类非本体像素：
- **深色文字**：`r<105 & g<105 & b<105`（郡名、国名）
- **白色线/点**：`r>190 & g>190 & b>190`（郡界、城市点）
- **红色标签**：HSV(`h<=18|h>=165`, `s>=45`) 加 RGB 兜底（抗锯齿红字）

### 1.3 迭代条件填充（关键技巧）
只填**被绿色包围**的缺陷像素，外部背景绝不动：
```python
for _ in range(100):
    green_frac = 7x7 邻域绿色占比
    candidate = 缺陷 & 尚未填充 & (green_frac > 0.30)
    if 无候选: break
    mask[candidate] = 255
```
这样内部文字/白线被逐圈"长"满绿色，而州外文字因周围没有绿色不会被误填。

### 1.4 形态学 + 连通域 + 补洞
- 7×7 闭运算桥接残余细缝 → 取最大连通域 → 外轮廓填充内部孔洞 → 5×5 高斯平滑边缘。

### 1.5 外轮廓简化
- `approxPolyDP` 自动搜 `eps`，使顶点数落在 **120~260**（太少丢细节，太多带锯齿）。

### 1.6 压边文字拉平（避免内凹凸起）★易踩坑
当郡名大字（如玄菟郡、辽东属国）正好压在州界上，被擦除后会在边界留下"内凹缺口"。处理原则：
- 找到与边界距离 < `pad` 的文字连通块 → 定位受影响的连续边界点段 → **直接用两端点插值成直线**替换该段。
- 只处理"中点在多边形外(内凹)"且"缺口 < 250px"的段，防止误切大海湾。
- **约定：一律拉平成直线，不要顺着文字轮廓做凹凸。**

### 1.7 去文字底图（必须用最终掩膜）★核心教训
> **最大的坑**：去文字图必须在**边界拉平之后**、用**最终 `mask_smooth`** 来填，否则拉平新纳入的区域里的文字不会被覆盖。
填充判据（掩膜内满足任一即填成平均绿）：
1. 与平均深绿色的欧氏色差 `> 35`（覆盖白/黑/棕/红字、道路网）
2. 原 defect 命中
3. 非"绿色主导"像素
最后 3×3 膨胀吃掉文字边缘抗锯齿。用 `verify_remaining_text.py` 校验掩膜内非绿像素应为 **0**。

### Step 1 产物（`<州>_step1/`）
- `mask_clean.png` — 二值掩膜
- `text_erased.png` — 去文字后的底图
- `boundary_overlay.png` — 原图叠红色州界
- `mask_with_boundary.png` — 掩膜叠边界
- `comparison_preview.png` — 原图/去文字/边界 三栏对比
- `boundary_points.json` — **边界像素点**（`seq, px, py`），供 Step 2/3

---

## 1★. Step 1 进阶方案：轮廓吸附法（Contour Snapping）— 冀州 v7c 最终版 ⭐推荐

> 幽州用的是「颜色阈值 + 迭代填充 + 压边拉平」（上一节）。这套方法在**州界压着大量文字、道路网、且相邻州白线互相干扰**时会反复翻车。冀州经过 v1→v7c 共十余次迭代，最终确立的 **轮廓吸附法** 更稳、更省事，**新州优先用这个**。
> 对应脚本：`step1_erase_text_jizhou_v7c.py`。

### 1★.1 核心思想：形状与位置分离
一句话：**用色块掩膜决定「形状/拓扑」，用白线决定「精确位置」，再把前者吸附到后者。**

- 色块掩膜（青色）→ 保证州的大致形状和面积正确，不会出现手指、缺块等拓扑错误；
- 白线边界像素 → 给出精确的边界线位置，不会跟着路网/文字跑偏；
- 对色块轮廓的每个点，找最近的白线像素并**吸附**过去 → 形状对、位置也对。

**关键优势：完全不依赖白线的连通性**——白线即使被文字/城市点打断成几百片碎段也无所谓（这正是 v7/v7b 失败的死穴，见 1★.5）。

### 1★.2 六个阶段
| 阶段 | 做什么 | 关键代码/参数 |
|---|---|---|
| **P1 色块掩膜** | 取本州青色，排除邻州（并/司/幽/青） | `cyan_raw` 阈值 + `~(邻州)`；`CLOSE(9)`→`OPEN(7)` 轻清理 |
| **P2 白线侧判定** | 白色像素 + Sobel 梯度>12（排除白背景内部）；8 邻域「一侧青一侧非青」→ 真边界像素 | `border_px = white_line & (1≤cyan邻居数≤7)` |
| **P3 色块外轮廓** | 取最大连通域外轮廓作为「粗多边形」 | `findContours(RETR_EXTERNAL, CHAIN_APPROX_NONE)` |
| **P4 ★吸附★** | 对粗轮廓每点，KDTree 找最近白线像素；距离 ≤ `MAX_SNAP_DIST` 就吸附，否则保持原位 | `cKDTree`；`MAX_SNAP_DIST = 25`（px） |
| **P5 坐标平滑** | 对吸附后坐标一维高斯滤波消抖动 | `gaussian_filter1d(sigma=4)` |
| **P6 填充+清离群** | 填多边形；删 <2000px 的孤立连通块；`approxPolyDP(eps=0.002·周长)` 精简 | 冀州最终 **65 顶点** |

### 1★.3 为什么这样设计有效
- **P2 的梯度过滤**：白线本身有强梯度，而大片白色背景/湖面内部是平的（梯度≈0），`gmag>12` 一刀切掉背景白，只留下真正的线。
- **P4 的 `MAX_SNAP_DIST=25`**：既能把粗轮廓吸到附近白线上，又不会把某段没有白线的地方（如被文字彻底盖住的边界）硬拉到很远的错误白线上——超距就保留色块原位，安全兜底。
- **吸附成功率**：冀州 100% 的轮廓点都在 25px 内找到了白线（14,495 个白线边界像素），说明色块与白线本就高度贴合，吸附只是「校正」而非「重构」。

### 1★.4 换州要改的
1. **P1 青色阈值 + 邻州排除**：这是唯一必调的部分。用取色脚本（如 `sample_jizhou_color.py`）采样目标州主色，并把周边相邻州的颜色写进 `~(...)` 排除项。
2. 若目标州白线更碎/更密，可微调 `MAX_SNAP_DIST`（一般 20~30 够用）和平滑 `sigma`。
3. 输出 JSON 格式为 `{"points": [[x,y],...], "count": N}`（**注意与老方法的 `{seq,px,py}` 不同**）；overlay 脚本要兼容这两种格式。

### 1★.5 失败迭代速查（别再走弯路）★核心教训
| 版本 | 思路 | 为何失败 |
|---|---|---|
| v6e | 白线环 **∩ 色块掩膜** 取交集 | 交集把白线的精度又拉回色块的粗糙，等于白做——**绝不要把追踪出的白线再和色块求交** |
| v7 | 白线像素直接取最大连通分量当轮廓 | 白线被文字/城市点打断成 **491 个碎片**，最大一片才 1596px，根本连不成环 |
| v7b | 距离变换「带宽法」桥接白线缺口 | 带宽 D=18 为了连断点，结果覆盖了 **286 万 px ≈ 整张图**，边界糊成一团 |
| **v7c** | **色块给形状 + 白线给位置 + KDTree 吸附** | ✅ 成功。绕开连通性问题，形状与位置各司其职 |

> **一句话教训**：白线追踪的正确姿势不是「把白线连成环」，而是「把已知形状吸附到白线上」。连通性是陷阱，吸附是出路。

### Step 1（v7c）产物（`jizhou_step1_v7c/`）
- `00_cyan_mask.png` — 清理后的青色形状掩膜
- `00_border_px.png` — 侧判定出的白线边界像素
- `mask_clean.png` — 最终二值掩膜
- `boundary_points.json` — 最终边界点 `{"points":[[x,y]...],"count":N}`
- `boundary_overlay.png` / `verify_overlay.png` — 原图叠红色州界 / 半透明填充核对
- `debug_rough_vs_snapped.png` — 绿=粗轮廓、红=吸附后，直观看吸附效果
- `comparison_preview.png` — 原图/抠图/边界 三栏对比

---

## 2. Step 2：地理配准（像素 → 经纬度）

### 锚点表格式 `<州>_anchor_table.json`
一组人工标注的"同名地物"，海岸线拐点/河口/半岛尖端最可靠：
```json
[
  {"name": "老铁山(辽东半岛西南端)", "type": "coast", "lon": 121.0, "lat": 38.74, "px": 1251, "py": 852},
  {"name": "渤海湾口(天津外海)",    "type": "coast", "lon": 118.0, "lat": 38.65, "px": 636,  "py": 898}
]
```
- **幽州用了 27 个锚点**，覆盖辽东半岛、渤海湾、朝鲜半岛西岸等。
- 锚点越多、分布越均匀，配准越稳。优先选海岸线特征（古今变化小）。

### 拟合
- 脚本同时拟合**一次(仿射)** 和 **二次多项式**，比较平均残差自动择优。
- 幽州：仿射 23.5km / 二次 23.0km，采用二次。**残差 20~25km 属正常**（范例图本身是手绘示意，非精确投影）。

### 换州要做的
为新州**重新标一套锚点表**（像素坐标在新范例图上，经纬度查现代地图）。这是 Step 2 唯一的人工工作量。

---

## 3. Step 3：映射并叠加到地形图核对

对应 `overlay_youzhou_step1.py`（全图）+ `zoom_overlay.py`（局部放大）。

### 大地形图映射参数（固定，勿改）
```python
BW, BH = 15600, 9600          # china_full_v3.png 尺寸
LON0, LON1 = 75.0, 140.0
LAT0, LAT1 = 15.0, 55.0
```

### 流程
边界点 `(px,py)` → 多项式 → `(lon,lat)` → `geo_to_big` → 地形图像素 → 缩放到预览 2600px → 画半透明多边形。

### 可调参数（脚本顶部）
| 参数 | 含义 | 幽州最终值 |
|---|---|---|
| `SHIFT_DEG_LON` | 整体经度平移，正=向东/右 | `0.75` |
| `SHIFT_DEG_LAT` | 整体纬度平移，正=向北/上 | `0.30` |
| 边界线宽 `bw` | `make_overlay` 里 | `3`（细线） |
| `fill_alpha` | 填充透明度，出 50/70/90 三档 | 主档 70 |

> **人工微调经验**：范例图是示意画，配准后整体可能有系统性偏移。用 `SHIFT_DEG_LON/LAT` 平移对齐一个可靠地标（幽州以**辽东半岛**对齐为准），比反复调锚点省事。**改 overlay 脚本时，`zoom_overlay.py` 里的同名偏移量要同步改**，否则放大图裁偏。

### Step 3 产物（`<州>_overlay_*/`）
- `*_a70.png / a50.png / a90.png` — 全境三档透明度
- `*_a70_zoom_large.png / *_a90_zoom.png` — 该州局部放大核对图

---

## 4. 下一个州 · 操作清单（Checklist）

1. **准备**：把 `汉末十三州地图范例/<新州>.png` 确认到位。
2. **复制脚本**：`step1_erase_text_youzhou.py` → `step1_erase_text_<新州>.py`，改 `SRC` 和 `OUTDIR`。
3. **采样主色**：写个小取色脚本采样该州色块，改 1.1 的颜色阈值。
4. **跑 Step 1**，看 `comparison_preview.png`：
   - 色块有没有漏 / 邻州有没有被吃进来 → 调阈值
   - 文字有没有擦干净 → 看 `text_erased.png`，必要时调色差阈值 (>35)
   - 压边大字有没有内凹 → 调 `flatten_boundary_at_labels` 的 `pad/min_area`
   - 用 `verify_remaining_text.py` 确认掩膜内非绿像素为 0
5. **标锚点**：在新范例图上挑 20+ 个海岸/河口/地标，量像素坐标，查经纬度，写 `<新州>_anchor_table.json`。
6. **复制叠加脚本**，改输入路径，跑一次看残差（应 < ~30km）。
7. **微调** `SHIFT_DEG_LON/LAT` 对齐地标，`zoom_overlay.py` 同步。
8. **交付**：呈现 zoom 图给用户确认形状；产出 `boundary_points.json` + `anchor_table.json` 归档。

---

## 5. 血泪教训速查

- ❌ 去文字图在拉平前生成 → 拉平区残留文字。✅ 一律用**最终掩膜**填。
- ❌ 只按文件大小/颜色单条件判缺陷 → 棕灰文字漏网。✅ **色差>35 + defect + 非绿主导** 三重兜底。
- ❌ 顺着文字轮廓修边界 → 奇怪凹凸。✅ 压边段**直接拉直线**。
- ❌ 只改 overlay 不改 zoom → 放大图裁偏。✅ 两处偏移量**同步**。
- ❌ 想靠调锚点消除系统性偏移 → 越调越乱。✅ 先配准，再用**整体平移**对齐一个可靠地标。
- ❌ 白线压满文字/路网时硬用颜色阈值+迭代填充 → 反复翻车。✅ 改用 **1★ 轮廓吸附法**（色块给形状、白线给位置）。
- ❌ 白线追踪时想「把碎白线连成环」（v7 取连通分量 / v7b 距离变换带宽）→ 要么连不上、要么糊满全图。✅ **KDTree 吸附**，不依赖连通性。
- ❌ 把追踪出的白线再 **∩ 色块掩膜**（v6e）→ 精度被拉回粗糙。✅ 两者只在「吸附」这一步耦合，不求交。
- ℹ️ 残差 20~25km 正常，范例图是手绘示意，别追求 0 误差。
- ℹ️ 标签**禁止**加白底/半透明背景色块（项目铁律），全用描边/阴影。
